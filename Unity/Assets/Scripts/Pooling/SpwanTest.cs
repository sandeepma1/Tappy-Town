using UnityEngine;
using System.Collections;

public class SpwanTest : MonoBehaviour {

  //	public  GameObject cube;
  //	GameObject g1, g2, g3;
  //	// Use this for initialization
  //	void Start ()
  //	{
  //		//g1 = ObjectPool.instance.GetObjectForType ("Cube", true);
  ////		print ("sfad");
  //		/*g1 = SimplePool.Spawn (cube, Vector3.zero, new Quaternion (0, 0, 0, 0));
  //		g2 = SimplePool.Spawn (cube, Vector3.zero, new Quaternion (0, 0, 0, 0));
  //		g3 = SimplePool.Spawn (cube, Vector3.zero, new Quaternion (0, 0, 0, 0));*/
  //		//SimplePool.Preload (cube, 5);
  //	}

  //	// Update is called once per frame
  //	void Update ()
  //	{
  //		if (Input.GetMouseButtonDown (0)) {
  //			g1.transform.position = Vector3.zero;
  //			//g3 = SimplePool.Spawn (cube, Vector3.zero, new Quaternion (0, 0, 0, 0));
  //		}
  //		if (Input.GetMouseButtonDown (1)) {
  //			/*SimplePool.Despawn (g1);*/
  //		}
  //	}
}
